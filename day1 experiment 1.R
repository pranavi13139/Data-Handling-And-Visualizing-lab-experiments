weight <- c(60, 65, 70, 72, 75)
height <- c(170, 175, 168, 180, 178)

plot(weight, height,
     main = "Weight vs Height",
     xlab = "Weight (kg)",
     ylab = "Height (cm)",
     pch = 19,
     col = "blue")
